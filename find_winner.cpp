#include <iostream>
#include <vector>
using namespace std;

struct cqueue {
	int front, size;
};

class Solution {
public:
	int getWinner(vector<int>& arr, int k) {
		int size = arr.size() - 1, value, freq;
		for (int idx = 1; idx < arr.size(); idx++) {
			value = arr[pos % size + 1];
			if (arr[0] > value) {

			}
		}
	}
};

int main(int argc, char* argv[]) {
	Solution s;
	return 0;
}
